apiVersion: 1

providers:
  - name: edge-tasks
    orgId: 1
    folder: Edge Tasks
    folderUid: edge-tasks
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: false
    options:
      path: /etc/grafana/dashboards
